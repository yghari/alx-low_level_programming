#include "main.h"

/**
 * _isdigit - check nuber if digit or not.
 *@c : the number to be checked
 *
 * Return:  1 is digit.
 * return : 0 if n digit;
 */

int	_isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
}
